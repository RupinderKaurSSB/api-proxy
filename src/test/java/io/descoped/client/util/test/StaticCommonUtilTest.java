package io.descoped.client.util.test;

import org.junit.Test;

/**
 * @author Ove Ranheim (oranheim@gmail.com)
 * @since 22/11/2017
 */
public class StaticCommonUtilTest {

    @Test
    public void testOtherMethods() throws Exception {
//        CommonUtil mock = Mockito.mock(CommonUtil.class);
//        when(mock.getCurrentPath()).thenReturn(Paths.get("/tmp"));

//        PowerMockito.mockStatic(CommonUtil.class);
//        OngoingStubbing<Path> path = PowerMockito.when(CommonUtil.getCurrentPath()).thenReturn(Paths.get("/tmp"));
//        assertThat(path).toString().equals("/tmp");

//        CommonUtil mock = spy(new CommonUtil());
//        doReturn(Paths.get("/tmp")).when(mock).invokesStatic(CommonUtil.class).makeString();
//        assertThat(mock.bar()).isEqualTo("foo");
    }
}
